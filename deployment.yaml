apiVersion: apps/v1
kind: Deployment
metadata: 
  name: rp-pods
  lables: 
    app: myapp
    env: dev
  namespace: devnamespace
  annotations:
    myecrimage: 503427799577.dkr.ecr.us-east-2.amazonaws.com/dev/myownimage:latest
spec:
  miniReadySeconds: 6
  replicas: 4
  selector:
    matchExpressions:
      - key: web
        operator: In
        values:
          - yellowapp-pods
      - key: env
        operator: NotIn
        values: 
          - dev
      - key:  frontend
        operator: Exists
  stratergy: 
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavilable: 1
  template: 
    metadata:
      name: podlevellables
      lables:
        web: yellowllapp-pods
        env: prod
        frontend: web
    spec: 
      containers:
        - name: yellowimage
          image: hemanth65/hemanth-electricimage
          ports:
            - name: yellowapp-ports
              containerport: 80
              protocol: TCP
          startupprobe: 
            httpGet:
              path: "/"
              port: 80
          livenessprobe:
            tcpSocket:
              port: 80
          readinessprobe:
            exec:
              command:
                - echo hi
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
            limits:
              cpu: 300m
              memory: 300Mi
           



    